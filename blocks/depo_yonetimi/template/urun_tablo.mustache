<table class="generaltable">
    <thead>
    <tr>
        <th>Ürün Adı</th>
        <th>Adet</th>
        <th>İşlemler</th>
    </tr>
    </thead>
    <tbody>
    {{#urunler}}
        <tr>
            <td>{{name}}</td>
            <td>{{adet}}</td>
            <td>
                <a href="{{duzenle_url}}">Düzenle</a> |
                <a href="{{sil_url}}">Sil</a>
            </td>
        </tr>
    {{/urunler}}
    </tbody>
</table>
<br>
<a href="{{back_url}}" class="back-link">← Depolara dön</a>
<br>
<a href="{{ekle_url}}" class="btn btn-primary">+ Ürün Ekle</a>
